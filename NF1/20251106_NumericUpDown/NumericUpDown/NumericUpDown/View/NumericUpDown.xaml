<UserControl x:Class="NumericUpDown.View.NumericUpDown"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NumericUpDown.View"
             mc:Ignorable="d" 
             d:DesignHeight="50" d:DesignWidth="200">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Button Grid.Column="0" FontSize="30" VerticalAlignment="Stretch" VerticalContentAlignment="Center">-</Button>
        <Button Grid.Column="2" FontSize="30" VerticalAlignment="Stretch" VerticalContentAlignment="Center">+</Button>
        <TextBox Grid.Column="1" 
                 FontSize="40"
                 Text="{Binding 
            RelativeSource={RelativeSource 
            AncestorType=local:NumericUpDown}, 
            Path=Valor}" 
            VerticalAlignment="Stretch" HorizontalAlignment="Stretch" VerticalContentAlignment="Center" HorizontalContentAlignment="Center"></TextBox>
    </Grid>
</UserControl>
