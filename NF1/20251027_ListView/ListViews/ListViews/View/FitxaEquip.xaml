<UserControl x:Class="ListViews.View.FitxaEquip"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ListViews.View"
             mc:Ignorable="d" 
             d:DesignHeight="200" d:DesignWidth="300">
    <Grid Margin="10"  >
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"></ColumnDefinition>
            <ColumnDefinition Width="1*"></ColumnDefinition>
            <ColumnDefinition Width="1*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="1*"></RowDefinition>
        </Grid.RowDefinitions>
        <Image Width="100" 
               Source="{Binding  
                                RelativeSource={RelativeSource AncestorType=local:FitxaEquip}, 
                                Path=TheEquip.UrlLogo,
                                FallbackValue='https://a.espncdn.com/combiner/i?img=/i/teamlogos/nba/500/min.png'}"
               Grid.Column="0" Grid.Row="0" Grid.RowSpan="2" ></Image>
        <TextBlock Padding="5" FontWeight="ExtraBold" TextWrapping="Wrap"
                   Text="{Binding 
                        RelativeSource={RelativeSource AncestorType=local:FitxaEquip}, 
                        Path=TheEquip.Nom,
                        FallbackValue=Timberwolves}" Grid.Column="1" Grid.Row="0"></TextBlock>

        <TextBlock Padding="5" Grid.Column="1" Grid.Row="1"  HorizontalAlignment="Center" 
                   TextWrapping="Wrap" 
                   Text="{Binding 
                            RelativeSource={RelativeSource AncestorType=local:FitxaEquip}, 
                            Path=TheEquip.ReducedDesc,
            FallbackValue='Lorem ipsum dolor' }" ></TextBlock>

        <ListView
            x:Name="lsvJugadors"
            ItemsSource="{Binding 
            RelativeSource={RelativeSource AncestorType=local:FitxaEquip}, 
            Path=TheEquip.Jugadors
            }"
            Grid.Column="2"  
            Grid.Row="1"
            SelectionChanged="ListView_SelectionChanged"
         >
            <!-- Estil per aconseguir que totes les files ocupin el mateix-->
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment"
                            Value="Stretch"></Setter>
                 </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <local:FilaJugador TheJugador="{Binding}"></local:FilaJugador>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        
    </Grid>
</UserControl>
